# Priority countries

One file per priority country (max five weighted highest). Copy [`_country-template.md`](_country-template.md) for each.

Also apply to non-priority countries at lower effort; list them in a separate section inside each file or add optional `other-countries.md`.

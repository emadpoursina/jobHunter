---
run: run-jobhunter-012
work_item: grounding-verification
intent: html-grounded-apply-script
generated: 2026-08-12T09:09:10Z
mode: autopilot
---

# Implementation Walkthrough: Grounding verification

## Summary

Verification now includes a deterministic mocked CSR-shell response that proves
the fetcher returns `unusable` and `null` HTML instead of passing empty markup
to the LLM. The existing route self-check also confirms fallback generation
still returns a safe script with grounding metadata.

## Structure Overview

The focused fetcher self-check owns pure fetch/trim behavior using mocked
responses. The route self-check exercises the full local API path and confirms
that an unusable fetched page does not prevent generation.

## Files Changed

### Modified

| File | Changes |
|------|---------|
| `tools/job-collector/pipeline/pageFetcher.self-check.js` | Adds a mocked one-input CSR shell and asserts `unusable` fallback status. |

## Key Implementation Details

### 1. Deterministic fallback coverage

The fixture has one input and returns HTTP 200, matching the important CSR-only
failure mode: technically successful HTTP with insufficient server-rendered
form markup.

### 2. Manual ATS validation

The test report records the human checklist for Greenhouse, Lever, Workday,
Ashby, and auth-walled pages, including green/red field review, `console.table`,
and manual Submit behavior.

## Decisions Made

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Fallback fixture | Mocked one-input shell | Avoids live ATS dependence while matching the unusable heuristic. |
| Manual checks | Documented, not automated | These checks require a logged-in browser and must not submit applications automatically. |

## Deviations from Plan

None.

## Dependencies Added

None.

## How to Verify

1. **Run deterministic fetcher checks**

   ```bash
   cd tools/job-collector && bun run pipeline/pageFetcher.self-check.js
   ```

   Expected: trim, usable, HTTP-error, unusable, and timeout assertions pass.

2. **Run route fallback check**

   ```bash
   PORT=3199 REPO_ROOT=/absolute/path/to/jobHunter bun run server/routes/apply.self-check.js
   ```

   Expected: the route returns `grounded: false`, `fetchStatus: unusable`,
   `pageHtml` in context, and a script containing `const SUBMIT = false`.

3. **Manual ATS checklist**

   - [ ] Greenhouse: matching fields fill green from grounded selectors.
   - [ ] Lever: React-controlled fields accept native setter events.
   - [ ] Workday: CSR shell falls back without a broken script.
   - [ ] Ashby: fields missing from the snapshot use per-field fallback.
   - [ ] Auth-walled page: fetch failure falls back without a route crash.
   - [ ] Review `console.table` and click Submit manually only after review.

## Test Coverage

- Tests added: 1 deterministic fallback assertion.
- Coverage: Not instrumented.
- Status: Passing.

## Ready for Review

- [x] Acceptance criteria met
- [x] Tests passing
- [x] No critical issues
- [x] Manual checklist documented
- [x] Developer notes captured

## Developer Notes

A 200 response is not proof of a usable form. The `unusable` status is
deliberately distinct from `http-error` so later capture decisions can
distinguish CSR-heavy ATS pages from blocked requests.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-jobhunter-012*

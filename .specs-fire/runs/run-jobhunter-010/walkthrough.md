---
run: run-jobhunter-010
work_item: funnel-stage-ui
intent: application-funnel-stages
generated: 2026-08-10T20:38:00Z
mode: confirm
---

# Implementation Walkthrough: Application funnel stages (run-jobhunter-010)

## Summary

Completed the confirm-mode batch: SQLite `application_stage`, API filter/validate/mark-applied wiring, and Jobs/JobDetail UI for funnel tracking. Collector `status` stays for pipeline processing; funnel outcomes live on `application_stage`.

## Structure Overview

```text
jobs.status          → raw | parsed | unmatched | cv_generated
jobs.application_stage → not_started … sent … offer | rejected | withdrawn
markApplied / stage=sent → stamps applied_at (first-wins)
```

## Files Changed

### Modified (this run)

| File | Changes |
|------|---------|
| `server/db.js` | Column, migrate, filter, markApplied, validation |
| `server/routes/jobs.js` | GET/PATCH stage support |
| `server/db.self-check.js` | Mapping + API asserts |
| `frontend` Jobs/JobDetail/JobCard/StatusBadge/api/Dashboard/css | Stage UX |
| `scripts/e2e-test.mjs` | Assert stage mapping |

## Decisions Made

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Legacy `status=applied` writes | Map to `application_stage=sent` | UI cutover without breaking bulk/old callers |
| `sent` stamps `applied_at` | Always (first-wins) | Consistent with mark-applied |
| Status filter | Drop applied/rejected | Stage filter is the funnel view |

## Deviations from Plan

None material. Duplicate `</section>` from JobDetail edit fixed in review.

## Dependencies Added

| Package | Why Needed |
|---------|------------|
| (none) | |

## How to Verify

1. **Self-check**
   ```bash
   cd tools/job-collector && bun run server/db.self-check.js
   ```
   Expected: OK

2. **UI** — open Jobs: Stage filter present; cards show stage badge; Job detail can change stage and Mark applied sets sent.

## Ready for Review

- [x] All acceptance criteria met
- [x] Tests passing
- [x] No critical issues
- [x] Documentation updated (run-009)
- [x] Developer notes captured

## Developer Notes

Docs already point at job-collector (run-009). No `application_stage_history` in this intent.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-jobhunter-010*

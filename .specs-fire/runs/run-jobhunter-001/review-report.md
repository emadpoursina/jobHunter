---
run: run-jobhunter-001
work_item: profile-parser
intent: linkedin-easy-apply-filler
generated: 2026-07-28T08:38:00Z
status: pass
---

# Code Review Report: Profile parser + cache

## Summary

| Category | Auto-fixed | Suggestions | Skipped |
|----------|------------|-------------|---------|
| Code Quality | 3 | 0 | 0 |
| Security | 0 | 0 | 0 |
| Architecture | 0 | 0 | 0 |
| Testing | 0 | 0 | 0 |

## Files Reviewed

- `tools/job-collector/pipeline/profile.js` (created)
- `tools/job-collector/pipeline/profile.self-check.js` (created)

## Auto-Fixed Issues

### 1. Unused `readProfileRaw` export — `pipeline/profile.js`
- **Category**: Code Quality
- **Severity**: low
- **Before**: `export async function readProfileRaw(...)` — exported but no consumer; self-check reads via `fs.readFile` directly.
- **After**: Removed. Per ponytail rule "deletion over addition".
- **Rationale**: Unused code is dead code. Removing it shrinks the module's surface and the locked contract.

### 2. Unused `readFile`, `resolve` imports — `pipeline/profile.js`
- **Category**: Code Quality
- **Severity**: low
- **Before**: `import { readFile, stat } from 'fs/promises'; import { resolve } from 'path';`
- **After**: `import { stat } from 'fs/promises';` (only `stat` remains used in `getParsedProfile`).
- **Rationale**: Unused imports are noise; linters flag them.

### 3. Unused `stat` import — `pipeline/profile.self-check.js`
- **Category**: Code Quality
- **Severity**: low
- **Before**: `import { readFile, stat } from 'fs/promises';`
- **After**: `import { readFile } from 'fs/promises';`
- **Rationale**: Same as above.

## Re-test After Auto-Fix

Self-check re-run after fixes: **PASS** (8/8 checks).

## Suggestions Requiring Approval

(none)

## Standards Compliance

- ✅ ESM, 2-space indent, single quotes, semicolons — matches `pipeline/cv.js`, `pipeline/repoFiles.js`
- ✅ Import order: builtins → local modules
- ✅ Error code `PROFILE_INCOMPLETE` follows `httpError`-style UPPER_SNAKE convention
- ✅ camelCase API surface, no DB schema change
- ✅ No new dependencies
- ✅ No secrets / PII in logs (profile is read on demand, not logged)
- ✅ Constitution: master-profile is source of truth — parser reads it verbatim, never invents

## Notes

- The `KEY_ALIASES` bug (missing `languages` entry) was caught by the self-check during implementation, not during review. This validates the ponytail rule's "leave one runnable check behind" — the check earned its keep on first run.
- `setSetting(TEMP_KEY, null)` in the self-check leaves a `null` JSON value in the settings table (no DELETE in the schema). Acceptable for a local single-user DB; flagged here for transparency.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-jobhunter-001*

---

# Code Review Report: CV md→PDF conversion

## Summary

| Category | Auto-fixed | Suggestions | Skipped |
|----------|------------|-------------|---------|
| Code Quality | 1 | 0 | 0 |
| Security | 0 | 0 | 0 |
| Architecture | 0 | 0 | 0 |
| Testing | 0 | 0 | 0 |

## Files Reviewed

- `tools/job-collector/pipeline/cvPdf.js` (created)
- `tools/job-collector/pipeline/cvPdf.self-check.js` (created)

## Auto-Fixed Issues

### 1. Unused `extname` import — `pipeline/cvPdf.js`
- **Category**: Code Quality
- **Severity**: low
- **Before**: `import { extname } from 'path';`
- **After**: removed (line deleted).
- **Rationale**: `pdfPathFor` uses a regex replace on the extension, not `extname`. Unused import.

## Re-test After Auto-Fix

Self-check re-run after fix: **PASS** (3/3 checks, 80,092-byte PDF produced).

## Suggestions Requiring Approval

(none)

## Standards Compliance

- ✅ ESM, 2-space indent, single quotes, semicolons — matches `pipeline/cv.js`, `pipeline/profile.js`
- ✅ Import order: builtins → external (`marked`) → local (`collectors/linkedin.js`, `repoFiles.js`)
- ✅ Error code `CV_PDF_ERROR` follows UPPER_SNAKE convention
- ✅ No new dependencies — reuses `marked` + Playwright from existing `package.json`
- ✅ No secrets / PII logged (only file path + byte count)
- ✅ Constitution: master-profile untouched; CV grounded in already-generated `.md` from `pipeline/cv.js`

## Notes

- The self-check writes a real PDF into `phase2/documents/generated/` (80KB). This is a side effect of running the check, not test pollution — the PDF is a legitimate artifact the user may keep. Flagged for transparency.
- Browser context is created/closed per conversion via `browser.newContext()` + `context.close()` in `finally`. The shared browser singleton from `getBrowser()` is reused, not relaunched.

---
*Appended for run-jobhunter-001, work item cv-md-to-pdf*

---

# Code Review Report: Application status tracking

## Summary

| Category | Auto-fixed | Suggestions | Skipped |
|----------|------------|-------------|---------|
| Code Quality | 0 | 0 | 0 |
| Security | 0 | 1 (non-blocking, noted) | 0 |
| Architecture | 0 | 0 | 0 |
| Testing | 0 | 0 | 0 |

## Files Reviewed

- `tools/job-collector/server/db.js` (modified)
- `tools/job-collector/server/routes/jobs.js` (modified)
- `tools/job-collector/server/db.self-check.js` (created)

## Auto-Fixed Issues

(none — changes follow existing patterns cleanly)

## Suggestions Requiring Approval

### 1. [Security, non-blocking] `addColumnIfMissing` interpolates identifiers into SQL — `server/db.js:125`
- **Category**: Security (low risk)
- **Severity**: informational
- **Current**: `sqlite.run(\`ALTER TABLE ${table} ADD COLUMN ${column} ${type}\`)` — string interpolation of `table`, `column`, `type`.
- **Risk**: low — function is not exported, only called with hardcoded literals (`'jobs'`, `'applied_at'`, `'TEXT'`). No user input reaches it.
- **Suggestion**: none required for v1. If the helper ever accepts external input, switch to a whitelist or assert identifiers match `/^[a-z_]+$/i` before interpolation. Flagged for awareness only.
- **Decision**: not applying — ponytail rule (no abstraction not requested; current callers are all literals).

## Standards Compliance

- ✅ ESM, 2-space indent, single quotes, semicolons — matches existing `db.js`
- ✅ snake_case DB columns (`applied_at`, `applied_url`), camelCase API (`appliedAt`, `appliedUrl` via `toCamel`)
- ✅ Error codes UPPER_SNAKE (`VALIDATION_ERROR`, `NOT_FOUND`) — reused from existing route patterns
- ✅ `markApplied` is idempotent (first-application-wins) — verified by self-check
- ✅ Migration is additive — no existing column dropped, no data rewritten; 8 rows preserved
- ✅ Route reuses `parseJobId` + 404/400 patterns already in `jobs.js`
- ✅ `status` field semantics unchanged — `applied_at` is a separate authoritative timestamp
- ✅ No new dependencies
- ✅ No secrets / PII logged

## Notes

- The self-check inserts and deletes a real row in the live `data/jobs.db`. The `try/finally` ensures cleanup even on assertion failure. The `source_url` includes a timestamp to avoid the unique-index dedup.
- The migration runs on every server boot via `migrate()`. The `addColumnIfMissing` guard makes it a no-op on subsequent boots.

---
*Appended for run-jobhunter-001, work item application-status-tracking*

---

# Code Review Report: Apply-form agent prompt

## Summary

| Category | Auto-fixed | Suggestions | Skipped |
|----------|------------|-------------|---------|
| Code Quality | 1 | 0 | 0 |
| Security | 0 | 0 | 0 |
| Architecture | 0 | 0 | 0 |
| Testing | 0 | 0 | 0 |

## Files Reviewed

- `docs/agents/apply-form.md` (created)
- `tools/job-collector/pipeline/applyForm.self-check.js` (created)

## Auto-Fixed Issues

### 1. Unused `readFile` import — `pipeline/applyForm.self-check.js:10`
- **Category**: Code Quality
- **Severity**: low
- **Before**: `import { readFile } from 'fs/promises';`
- **After**: removed. The file uses `readRepoFile` from `./repoFiles.js` instead.
- **Rationale**: Unused import; the self-check reads the agent file via `readRepoFile` (same pattern as `pipeline/cv.js`).

## Re-test After Auto-Fix

Self-check re-run after fix: **PASS** (6/6 structural checks; LLM round-trip skipped on 5s timeout).

## Suggestions Requiring Approval

(none)

## Standards Compliance

- ✅ `docs/agents/apply-form.md` follows the structural pattern of `docs/agents/cv-generator.md` (system prompt producing a single artifact, "output the artifact only" footer).
- ✅ Self-check follows ponytail rule: single runnable script, no test framework, skips gracefully on flaky external dependency (LLM).
- ✅ ESM, 2-space indent, single quotes, semicolons.
- ✅ Import order: builtins → local modules.
- ✅ Constitution: master-profile is source of truth — the prompt reads profile fields from `__APPLY_CTX__` (injected by the route from the parsed profile), never invents.
- ✅ Constitution: "AI drafts require 100% human review before send" — the script defaults `SUBMIT = false` and leaves the Review step for the human to click.
- ✅ Constitution: "Job-collector scrapers must respect site terms; prefer manual/authenticated flows" — the script runs in the user's own logged-in browser (paste-into-DevTools), not headless, and does not auto-submit.
- ✅ No new dependencies.

## Notes

- The prompt is intentionally long (7,581 chars) because it must constrain the LLM on safety (no auto-submit, no EEO), robustness (try/catch per step, text-based button matching), and the `__APPLY_CTX__` injection contract. Brevity here would risk the LLM emitting a script that auto-submits or answers demographic questions.
- The self-check's LLM round-trip could not be validated against the configured free OpenRouter model (empty/timeout). The structural checks are the authoritative validation for this work item. The route (work item #5) will exercise the prompt end-to-end with a real job.

---
*Appended for run-jobhunter-001, work item apply-form-agent-prompt*

---

# Code Review Report: Apply script API route + UI button

## Summary

| Category | Auto-fixed | Suggestions | Skipped |
|----------|------------|-------------|---------|
| Code Quality | 0 | 0 | 0 |
| Security | 0 | 0 | 0 |
| Architecture | 0 | 0 | 0 |
| Testing | 0 | 0 | 0 |

## Files Reviewed

- `tools/job-collector/server/routes/apply.js` (created)
- `tools/job-collector/server/routes/apply.self-check.js` (created)
- `tools/job-collector/server/index.js` (modified)
- `tools/job-collector/frontend/src/api.js` (modified)
- `tools/job-collector/frontend/src/pages/JobDetail.jsx` (modified)

## Auto-Fixed Issues

(none — all changes follow existing patterns)

## Suggestions Requiring Approval

(none)

## Standards Compliance

- ✅ `server/routes/apply.js` follows the route-module pattern of `server/routes/pipeline.js` (Router, `asyncHandler`, explicit `res.status().json()` for known errors, `callLlm` for LLM work).
- ✅ Mounted in `server/index.js` via `app.use('/api/apply', applyRouter)` — same pattern as other routers.
- ✅ Error codes UPPER_SNAKE: `VALIDATION_ERROR`, `NOT_FOUND`, `PROFILE_INCOMPLETE`, `CV_NOT_GENERATED`, `CV_PDF_ERROR`, `LLM_ERROR`, `INTERNAL_ERROR`.
- ✅ Import order in `apply.js`: builtins → external → local (matches coding standards).
- ✅ Frontend `api.js` additions follow the existing `request(method, path, body)` pattern.
- ✅ `JobDetail.jsx` additions reuse existing `copyToClipboard` + `showAlert` helpers (same pattern as the "Copy path" button).
- ✅ New state vars (`generatingApplyScript`, `markingApplied`) follow the existing `useState` pattern.
- ✅ No new dependencies (backend or frontend).
- ✅ Constitution: `SUBMIT = false` default enforced by the agent prompt (work item #4); the route prepends `__APPLY_CTX__` so the script is self-contained; the user clicks Submit themselves.
- ✅ Constitution: "Job-collector scrapers must respect site terms; prefer manual/authenticated flows" — the script runs in the user's own logged-in browser (paste-into-DevTools), not headless.
- ✅ No secrets / PII logged (only job id + script length).

## Notes

- The route is synchronous (not queued through `p-queue`), a deliberate deviation from the work item's suggestion. Rationale: single LLM call, frontend awaits with a loading state, smaller diff than queue+poll. Documented in the plan and test report.
- `generateAnswers` is best-effort: any failure returns `{}`, and the script highlights unknown questions red either way. This makes the route resilient to LLM flakiness for the answer-pre-generation step while still requiring a working LLM for the main script generation.
- The self-check starts a real server on port 3099 (non-default to avoid clashing with a running dev server). It cleans up the server in a `finally` block.
- The "Mark applied (Easy Apply)" button disables itself when `job.appliedAt` is already set, mirroring the idempotency of the `markApplied` DB helper (work item #3).

## Re-test After Review

No fixes applied (nothing to fix). Self-check and frontend build both pass.

---
*Appended for run-jobhunter-001, work item apply-script-route-and-ui (final item)*

---
run: run-jobhunter-005
work_item: profile-ai-update-flow
intent: master-profile-editor
generated: 2026-08-07T21:42:30Z
mode: validate
---

# Implementation Walkthrough: AI profile update flow and task configuration

## Summary

Added write-free AI preview and explicit apply endpoints for master profile updates, backed by a dedicated agent prompt and `profile_update` LLM task configuration. Profile reads/writes now include SHA-256 revision fingerprints for optimistic locking; stale proposals are rejected with `PROFILE_REVISION_CONFLICT`.

## Structure Overview

Profile routes delegate to `profileEditor.js` for AI orchestration and `profile.js` for filesystem access, validation, hashing, and atomic persistence. Settings exposes the new task override alongside existing parse/CV/cover-letter tasks.

## API Contract (for Profile UI work item)

| Endpoint | Body | Response |
|----------|------|----------|
| `GET /api/profile` | — | `{ markdown, revision }` |
| `PUT /api/profile` | `{ markdown, baseRevision? }` | `{ markdown, revision }` |
| `POST /api/profile/ai-preview` | `{ prompt, baseRevision? }` | `{ proposal, baseRevision }` |
| `POST /api/profile/ai-apply` | `{ proposal, baseRevision }` | `{ markdown, revision }` |

## Files Changed

### Created
- `pipeline/profileEditor.js` — preview/apply orchestration
- `docs/agents/profile-editor.md` — agent system prompt
- `pipeline/profileAiUpdate.self-check.js` — mocked LLM checks

### Modified
- `pipeline/profile.js` — revision hash, atomic save
- `server/routes/profile.js` — AI routes + revision shape
- `server/db.js`, `Settings.jsx` — `profile_update` task
- `frontend/src/api.js` — client methods

## Security Considerations

| Concern | Approach |
|---------|----------|
| Stale AI overwrite | Server revision fingerprint required on apply |
| Data loss on write | Validate then atomic temp-rename |
| PII in logs | No profile/prompt body logging |

## Deviations from Plan

None.

## Dependencies Added

None.

## How to Verify

```bash
cd tools/job-collector
bun run pipeline/profileAiUpdate.self-check.js
bun run pipeline/profileApi.self-check.js
```

## Test Coverage

29 assertions across three self-check scripts — all passing.

## Developer Notes

- AI preview operates on the **saved** server profile; the Profile UI must save or discard unsaved editor state before preview.
- `sanitizeProfileProposal` trims outer whitespace (including trailing newline) from LLM output.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-jobhunter-005*
